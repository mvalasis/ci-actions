{
  "name": "ci-actions-test-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Per-stack test-suite CI gate — runs the repo's own Node/PHP tests, report-mode-first, with a 'no tests = green' floor. Zero runtime deps (Node built-ins only).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "selftest": "node scripts/selftest.mjs"
  }
}
