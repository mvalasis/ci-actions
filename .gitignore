# runtime artifacts produced by the action when run locally
__pycache__/
*.pyc
pages.txt
linkcheck-report.txt

# node deps installed at action runtime (regenerated via `npm ci` from the lockfile)
node_modules/
